'use strict';

angular.module('indiesoundsApp')
  .controller('ProfileSetCtrl', function ($scope) {
    $scope.message = 'Hello';
    
    // service.getProfile
    // 
    // $scope.submitProfile = service.submitProfile(id, profile)
  });
